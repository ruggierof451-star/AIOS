{
  "name": "@aios/api-contract",
  "version": "0.1.0",
  "private": true,
  "description": "Envelope di risposta, error model e codici errore condivisi (API Contract, Engineering Bible Modulo 2).",
  "main": "./dist/envelope.js",
  "types": "./dist/envelope.d.ts",
  "exports": {
    ".": {
      "types": "./dist/envelope.d.ts",
      "default": "./dist/envelope.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "typescript": "^5.5.0",
    "@types/node": "^20.14.0"
  }
}
